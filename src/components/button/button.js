import React from 'react';
import Button from '@mui/material/Button';
import './button.css';

const MyButton = () => {
  return (
    <Button variant="outlined" className="whiteButton">
      <p>Contact Now</p>
    </Button>
  );
};

export default MyButton;
